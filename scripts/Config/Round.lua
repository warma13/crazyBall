-- ============================================================================
-- Config/Round.lua - 主线轮次系统与抽取配置
-- ============================================================================

local M = {}

-- 轮次系统配置
M.ROUND = {
    TIME_LIMIT = 60,
    BASE_TARGET = 20,
    GROWTH_RATE = 1.28,
}

-- 抽取费用
M.DRAW_COST = 3
M.DRAW_COST_MULTIPLIER = 1.3

return M
